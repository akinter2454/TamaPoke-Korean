# TamaPoke v3.96.1 빌드 검증 핫픽스

## 원인
- v3.96.0의 `tools/verify_card_evolution.py`가 C++ 의미가 아니라 정확한 공백 문자열 `choiceKind = 1`을 검사했습니다.
- 실제 소스는 동등한 코드인 `choiceKind=1`을 사용해 기능은 정상인데 GitHub Actions가 실패했습니다.

## 수정
- 카드 진화 검증을 공백에 독립적인 정규식/동작 기반 검사로 변경했습니다.
- 일반 진화와 조그레스가 별도 선택되는지, `Pet::jogress()`가 별도 경로인지 함께 검증합니다.
- FW_VERSION 및 설치기 버전을 3.96.1로 동기화했습니다.
- 게임의 진화 조건, 도감 데이터, 세이브 구조는 변경하지 않았습니다.
