# TamaPoke v3.98.0

## 수정 1 — 체육관/미션 계열 배틀 이름
전투용 `Combatant.name`은 12바이트이므로 한국어 기본 종 이름을 저장하면 UTF-8 글자가 중간에서 잘릴 수 있었습니다. v3.98.0부터 이 필드는 별명에만 사용하고 기본 종 이름은 `dex`로 표시 시점에 조회합니다. 체육관, 일반 미션/탐험, 배틀타워 및 동일 전투 엔진 경로에 공통 적용됩니다.

## 수정 2 — 키우기 슬롯 5개
`CARE_SLOT_COUNT`를 5로 확장하고 `care3/care4`, `c3a/c3b`, `c4a/c4b`를 추가했습니다. 기존 1~3번 슬롯은 유지되며 4~5번은 새 슬롯입니다. SAVE_VERSION은 2를 유지합니다. 중요 성장치 복구 가드는 5슬롯용 v3로 확장하되 기존 3슬롯 v2 가드를 읽어 마이그레이션합니다.
