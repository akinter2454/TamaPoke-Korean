# TamaPoke v3.97.2 — 다음 진화 조건 가독성

## 실기기 피드백 반영
v3.97.1에서 진화 조건 글씨의 최소 크기를 2배로 올렸지만, 알파몬 왕룡검처럼 긴 한 줄 조건은 실제 원형 화면에서 여전히 작게 보였습니다.

## v3.97.2 표시 규칙
- 한 줄 전체가 4배 크기로 들어가면 4배 표시
- 한 줄이 길고 `→`가 있으면 `조건` / `→ 결과` 두 줄로 분리
- 분리된 조건은 3배, 결과는 최대 4배 표시
- 조건부가 3배로도 너무 길면 `+`에서 추가 분리하여 3배 유지
- 기존 2~4개 분기 안내는 화면 수에 따라 2~3배 표시

예시:

기존:
`조그 L55 + 오류우몬L55 → 알파몬 왕룡검` (2배 한 줄)

변경:
`조그 L55 + 오류우몬L55` (3배)
`→ 알파몬 왕룡검` (최대 4배)

게임의 실제 진화/조그레스 조건은 변경하지 않습니다.
